export class Student {
  id: number;
  name: string;
  grade: number;
  level: string;

  constructor(id: number, name: string, grade: number, level: string) {
    this.id = id;
    this.name = name;
    this.grade = grade;
    this.level = level;
  }
}
